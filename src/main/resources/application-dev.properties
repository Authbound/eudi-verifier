# Dev-only overrides (do not use in production)
server.error.include-stacktrace=always

# permissive CORS for local development
cors.origins=*
cors.originPatterns=*
cors.methods=*
cors.headers=*
cors.credentials=false
cors.maxAge=3600
